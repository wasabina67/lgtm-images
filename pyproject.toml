[project]
name = "lgtm-images"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.12.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.9",
]
